# Raw Kubernetes Secret for Todo Chatbot Backend (FastAPI)
# For direct kubectl apply without Helm
# Generated by k8s-manifest-agent for reference/fallback deployment
#
# IMPORTANT: Replace CHANGE_ME values before applying!
# kubectl create secret generic todo-backend-secret \
#   --from-literal=DATABASE_URL="postgresql://..." \
#   --from-literal=BETTER_AUTH_SECRET="your-secret" \
#   --from-literal=OPENAI_API_KEY="sk-..."
apiVersion: v1
kind: Secret
metadata:
  name: todo-backend-secret
  labels:
    app.kubernetes.io/name: todo-backend
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: todo-chatbot
type: Opaque
stringData:
  # PostgreSQL connection URL (override before apply!)
  DATABASE_URL: "CHANGE_ME"
  # Better Auth JWT signing secret (override before apply!)
  BETTER_AUTH_SECRET: "CHANGE_ME"
  # OpenAI API key for AI features (override before apply!)
  OPENAI_API_KEY: "CHANGE_ME"
